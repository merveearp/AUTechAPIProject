@using AITech.WEBUI.DTOs.AboutItemDtos
@model List<ResultAboutItemDto>
@*
    _HomeAboutItemComponent
*@

<div class="row g-3">
    @foreach (var item in Model)
    {
        <div class="col-sm-6">
            <h6 class="mb-3">
                <i class="fa fa-check text-primary me-2"></i>
                @item.Title
            </h6>
        </div>
    }
</div>
